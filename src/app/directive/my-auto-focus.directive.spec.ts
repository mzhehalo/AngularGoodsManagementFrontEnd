import { MyAutoFocusDirective } from './my-auto-focus.directive';

describe('MyAutoFocusDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MyAutoFocusDirective();
    expect(directive).toBeTruthy();
  });
});
